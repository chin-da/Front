import { styled } from '@/styles/stitches.config';
export const TextWrapper = styled('p', {
  all: 'unset',
  fontWeight: '400',
  fontSize: '.875rem',
  lineHeight: '1.25rem',
  color: '$stone',
});
