import { styled } from '@/styles/stitches.config';

export const TitleWrapper = styled('p', {
  all: 'unset',
  fontWeight: '500',
  fontSize: '1.125rem',
  lineHeight: '1.75rem',
  color: '$black',
});
